name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

env:
  CARGO_TERM_COLOR: always

jobs:
  fmt:
    name: Formatting
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt
      - run: cargo fmt --all -- --check

  clippy:
    name: Clippy
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy
      - uses: Swatinem/rust-cache@v2
      - run: cargo clippy --all-targets --all-features -- -D warnings

  test:
    name: Test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2

      - name: Run unit tests
        run: cargo test --lib -- --quiet

      - name: Run E2E tests (Phase 0 + Phase 1 network)
        run: cargo test --test e2e -- --quiet

      - name: Run multi-node SC-* gates (Phase 1)
        run: cargo test --test multi_node -- --quiet

      - name: Run full test suite
        run: cargo test --all -- --quiet

      - name: Run Phase 0 shell script (additional E2E validation)
        if: matrix.os == 'ubuntu-latest'
        run: ./scripts/test-phase0.sh

      - name: Run Phase 1 network smoke script
        if: matrix.os == 'ubuntu-latest'
        run: ./scripts/test-phase1-network.sh

      - name: Build release binary
        run: cargo build --release

      - name: Upload binary (Linux)
        if: matrix.os == 'ubuntu-latest'
        uses: actions/upload-artifact@v4
        with:
          name: soal-linux
          path: target/release/soal

      - name: Upload binary (macOS)
        if: matrix.os == 'macos-latest'
        uses: actions/upload-artifact@v4
        with:
          name: soal-macos
          path: target/release/soal

      - name: Upload binary (Windows)
        if: matrix.os == 'windows-latest'
        uses: actions/upload-artifact@v4
        with:
          name: soal-windows
          path: target/release/soal.exe
